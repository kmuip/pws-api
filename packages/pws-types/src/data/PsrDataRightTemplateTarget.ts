export type PsrDataRightTemplateTarget = {
  // Core properties
  DataType?: string // Data type for the template
  Parent?: unknown // Parent of the target
  TagTemplates?: unknown[] // Tag templates configured for the target
  Target?: unknown // Target of the template
  Templates?: unknown[] // Templates configured for the target
}
