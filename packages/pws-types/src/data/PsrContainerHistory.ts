export type PsrContainerHistory = {
  __type?: string // Internally used, never change this value
  Container?: any // Historic container
  ItemHistoryBindings?: any[] // List of historic container items
}
